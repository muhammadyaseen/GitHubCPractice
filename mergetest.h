/*
 * test file for merge conflicts
 */

void printName(char *);
float add(float, float);

void printName(char* name)
{
    printf("\nThe given name is %s.\n", name);
}

float add(float x, float y)
{
    return (x+y);
}
